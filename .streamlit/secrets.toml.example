# Copy to .streamlit/secrets.toml locally, or paste into Streamlit Cloud → Settings → Secrets.
# Your Supabase Postgres connection string (Supabase → Project Settings → Database → Connection string → URI):
db_url = "postgresql://postgres:YOUR-PASSWORD@db.YOUR-PROJECT.supabase.co:5432/postgres"
