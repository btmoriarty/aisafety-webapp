# No custom [theme] here on purpose: a custom theme pins Streamlit to a light
# base and ignores the viewer's dark-mode preference. Leaving it out lets each
# viewer use Light / Dark / System (via the ⋮ menu). Accent styling is done in
# CSS in app.py so it works in both modes.
